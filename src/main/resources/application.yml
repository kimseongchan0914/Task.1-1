spring:
  application:
    name: task-1-1

  datasource:
    url: jdbc:mysql://localhost:3306/task11?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=utf8mb4
    username: root
    password: kimsungxhan1535
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080

jwt:
  secret: "your-secret-key-must-be-at-least-256-bits-long-for-hs256-algorithm-security-key-here-at-least-32-characters"
  expiration: 86400000